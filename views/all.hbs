<center>

  <h1>{{title}}</h1>
  <a href="/"> <b>Add Lead</b></a>
  <p>View Leads</p>

  <div>
    <table>
      {{#each data}}
      <tr>
        {{#eachProperty this}}
        <td>{{value}}</td>
        {{/eachProperty}}
        <td> <a href="/leads/{{this.id}}">View</a></td>
        <td> <a href="/leads/delete/{{this.id}}">Delete</a></td>
      </tr>
      {{/each}}
    </table>
  </div>
</center>